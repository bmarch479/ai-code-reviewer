# Frontend

This directory contains the Next.js frontend for the AI code reviewer application. It will provide a web interface to upload repositories and specify expected behavior, view test results, view proposed patches, etc.

## Setup

- Install dependencies with `npm install` or `yarn`.
- Run development server: `npm run dev`.
